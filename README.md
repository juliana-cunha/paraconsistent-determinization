# paraconsistent-determinization
This script implements the determinization process for FPNA whose transitions take values in the unit square $[0,1]^2$. It includes the examples from Section 3 and outputs the resulting deterministic automaton (FPDA).
